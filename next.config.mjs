/** @type {import('next').NextConfig} */
// RE:VIVE by Aether - Next.js Configuration
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
}

export default nextConfig
